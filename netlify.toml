# Netlify configuration file for Prediction Foot Africa

[build]
  command = "npm run build"
  publish = "dist"

# Single Page Application (SPA) redirect rule
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
